services:
  backend:
    container_name: D-Sektionen-Backend
    image: registry.d-sektionen.se/backend:latest
    restart: unless-stopped
    command: uwsgi --ini /etc/uwsgi/backend.uwsgi.ini
    volumes:
      # Set the paths when deploying to production.
      - ${BACKEND_BASEDIR}/backend.uwsgi.ini:/etc/uwsgi/backend.uwsgi.ini
      - ${BACKEND_BASEDIR}/static:/code/static
      - ${BACKEND_BASEDIR}/media:/code/media
      - ${BACKEND_BASEDIR}/log:/var/log/
    ports:
      # Update for production environment
      - 13337:8000
    environment:
      # Set these when deploying to production
      POSTGRES_NAME: backend
      POSTGRES_USER: backend
      POSTGRES_PASSWORD: XXXX
      POSTGRES_HOST: 172.17.0.1
      POSTGRES_PORT: 5432
    env_file:
      # Environment variables set in portainer gui.
      - stack.env
    networks:
      - default

networks:
  default:
    driver: bridge
    name: backend_network
